# Mニュース 開発ルール

## 進行ルール(プロジェクト共通)
- コストがかかるもの(有料API、有料プラン、外部サービス課金等)は勝手に判断せず、必ず事前に確認を取る
- 法的に問題のあることはしない
- セキュリティ的にリスクのある実装(認証なしの管理画面公開、秘密情報のクライアント露出等)は勝手に判断せず確認を取る
- 人間の判断が必要なものはブロックせず後回しにして、進められる部分を先に進める
- 利用量の管理(Vercel等のインフラ + Claude自体の利用枠の両方):
  - 週間/セッションの利用枠を序盤で使い切るペースにしない(例: 初日に週間枠の50%超えはNG)
  - 大きいタスクは一括でやらず、優先度順に区切って実行し、区切りごとに利用ペースを踏まえて続行可否を判断する
  - トークン消費が大きい作業は対象を絞る・サンプリングする
  - 利用量が想定より膨らみそうなら途中で止めて報告する

## 言語

ユーザーへの応答はすべて日本語で書く。調査結果の説明、着手前の
分析、選択肢の提示、確認の問い合わせ、レポートの本文も含めて
例外なく日本語とする。コード・コミットメッセージ・PRタイトル・
変数名・ログ出力は従来どおり英語でよい。

## 本番サービスの保護(夜間作業でも閲覧者がいる前提で扱う)
- 本番(www.mnews.jp)を落とさないこと
- mainブランチへの直接pushはしない。必ず作業ブランチ + Vercelのプレビューデプロイで動作確認してから本番反映する
- データ変更(選手DB・イベントデータ・結果データのスキーマ変更や一括更新)は実行前にバックアップを取り、切り戻し手順を用意してから行う
- 本番反映は「ビルドが通っている + プレビューで主要ページ(トップ/選手/イベント/結果/rankings)の表示確認済み」の場合のみ
- 本番反映後、主要ページが正常表示されているか確認してから次の作業に進む。エラー(ビルド失敗、500、白画面)が出た場合は直前のデプロイに即ロールバックし、原因調査はその後に行う
- マージ・デプロイの時間帯制約はなし。ただしsrc/を含む変更をマージした場合は、デプロイ完了後に主要ページ(トップ・選手一覧・イベント・結果・rankings)を確認し、異常があれば即revertする。

## デプロイ
- 通常: `git push origin main`。mainへのマージ(push)がそのまま本番への自動デプロイになる。**mainマージとデプロイは分離した工程ではない** — 上記「本番サービスの保護」の各ルールはマージ操作そのものに適用される
- `vercel deploy --prod` の直接実行は禁止。本番反映は必ずmainへのマージ経由で行う
- 上記の本番保護ルールに該当する変更(大きい機能追加・データ変更等)は作業ブランチ + `vercel deploy`(プレビュー、本番ではない)で確認してからmainにマージする
- **PR作成直前に最新mainへrebaseしてからpush**する。マージ前にconflictや前提のズレを解消しておく
- **積み木PR(他PRの未マージブランチをベースにするPR)は原則禁止**。並行作業は必ずmainから分岐する。やむを得ず積み木構成にした場合、下段のPRをマージする際にブランチを削除しない(上段PRのベースがなくなるため)
- **複数セッションが同一スコープに並行着手する事故の防止(2026-07-26判明)**: 何かに着手する前に、open な PR(draft含む)と `git worktree list` を確認し、同じスコープに既に着手されていないかを見る。着手済みなら作業せず報告して停止する。着手する側は、作業開始時点で**ブランチ名とスコープ1行だけのdraft PRを即座に開く**(実装が無くてもよい)。新しいロック機構は作らず、既存のGitHub(open PR一覧)をclaimの仕組みとして使う。**同一ブランチへforce-pushしない**。リモートが進んでいたら`git rebase`で統合してからpushする(2つのセッションが同じブランチを同時に触るケースはdraft PR claimだけでは防げないため)
- **1ブランチにつきworktreeは1つ**。同じブランチに対して複数のworktreeを作らない。作業開始前に `git worktree list` で対象ブランチのworktreeが既に存在しないか確認し、存在すれば別セッションが使用中の可能性があるため着手せず報告する。
- **同一ファイルを複数セッションが並行編集する衝突の防止(2026-08-01判明、`deepScraper.ts`で1日に4件発生)**: スコープ名(ブランチ名・PRタイトル)が違っていても、同じファイル(特に`src/lib/mnewsRating/deepScraper.ts`・`scripts/build-deep-records.ts`のように複数の指示書から触られやすいファイル)を複数セッションが同時に編集するとマージ時にコンフリクトやリベース地獄が起きる。着手前に `gh pr list --state open` で対象ファイルに関係しそうなPRのタイトル・ブランチ名を確認するだけでなく、可能なら `gh pr diff <番号>` で実際に触っているファイルを確認する(タイトルだけでは対象ファイルの重複に気づけないことがある)。同一ファイルへの並行着手が疑われる場合は、着手済みかどうかをスコープと同様に確認し、必要なら統合してから着手する。マージ時にコンフリクトが出た場合は手でのコンフリクト解消を避け、リベース後に該当スクリプト(バックフィル・データ生成スクリプト等)を再実行してdata/を作り直す(#302→#305、#309のリベース対応と同じ手順)。
- **`src/lib/mnewsRating/deepScraper.ts`・`scripts/build-deep-records.ts`・`data/deepRecords.json`は特に衝突が集中しやすいファイル(2026-08-01、1日で5件の並行着手事故: #310重複着手・#313でゲート方式変更・#309が修正なしで先行マージ・#307と#309の重複実装・#321の重複緊急修正)。この3ファイルのいずれかに着手する前は、上記の一般ルールに加えて必ず次の2つを実行してから着手すること: ①`gh pr list --state open`で該当ファイルを触っていそうなPR(タイトルだけで判断せず`gh pr diff <番号>`で対象ファイルを確認)が無いか、②`git worktree list`で同種のブランチ(`fix/deep-*`・`feat/deep-*`等)のworktreeが既に存在しないか。どちらかに該当があれば着手せず報告して停止する(スコープが違っていても同じファイルなら停止対象)。
- **同一ブランチへのforce-pushは禁止**(既存ルールの再掲)。リモートが進んでいたら `git rebase` で統合してからpushする。
- **git worktreeからデプロイする場合**: worktree直下には`.vercel/`が無く、そのまま`vercel deploy`すると誤って新規プロジェクトが作成される(2026-07-11に`mnews-worktree-fontfix`を誤作成→削除した実例あり)。プレビュー確認前に必ず本体の`.vercel/project.json`(`projectId: prj_BOiZsdSXZ5tEVMQ0DV8WmOl0c6pg`, `projectName: mnews`)をworktreeにコピーするか`vercel link`で本番プロジェクトを明示指定してから実行する
- **deployはISRキャッシュを自動パージしない**(2026-07-17判明)。`data/rankings.json`等のデータはGitHub raw経由でrevalidate:3600(最大1時間)のfetchキャッシュに乗っており、「mainにマージしただけで新規デプロイをしていない」場合は最大1時間古い値が残る。新規デプロイ自体もURLに埋め込まれたcommit SHAが変わるため次回アクセスで自動的に最新を取るが、即時反映させたい場合は`POST /api/revalidate-rankings`(`Authorization: Bearer <REVALIDATE_TOKEN>`、Vercel環境変数に別途設定が必要)を呼ぶ。**データ更新を伴うdeploy後はrevalidate必須**。反映確認は全公開階級(`/rankings/[division]`)でJSON-LDを取得し、最終更新日付が全ページ一致していることを見る。
- **`/fighters/[slug]`は日次バッチ完了から画面反映まで最悪約1時間15分**(2026-08-04、ISR化)。fetchのrevalidate(最大1時間) + ページのISR(900秒)が積み上がるため。日次バッチのコミットは`[skip ci]`でデプロイを起こさない(=fetchのキャッシュキーに使うcommit SHAが変わらない)ので、この遅延は毎回発生する。即時反映が必要な場合は再デプロイするとSHAが変わり両方のキャッシュが切れる。

## 定期実行ジョブ一覧
出典は`.github/workflows/*.yml`と`vercel.json`の実ファイルのみ(2026-07-25調査時点)。個別のcron時刻をコード外の記憶で補わない。

data/配下を触るPRをマージする前に、この表のジョブが実行中または待機中でないかを GitHub API で確認する。実行中の場合は完了を待ってからマージする。時間帯による制約はない。

| ジョブ | 種別 | cron (UTC) | JST | 書き込み先 | 備考 |
|---|---|---|---|---|---|
| `archive-articles.yml` | GHA schedule | `*/30 * * * *` | 常時(30分ごと) | `data/archive.json` | |
| `update-org-rankings.yml` | GHA schedule | `17 15 * * *` | 0:17 | `data/orgRankings.json`, `data/orgRankings-prev.json` | |
| `update-fighter-records.yml` | GHA schedule | `30 17 * * *` | 2:30 | `data/fighterRecords.json`, `data/rankings.json`, `data/rankings.prev.json`, `data/rankings/archive/*`, `data/rankings.legitimateBaseline.json` | 同一ジョブ内で`update-mnews-rating.ts`(mode未指定=new-results)も実行 |
| `/api/cron/countdown-post` | Vercel Cron | `0 11 * * *` | 20:00 | `data/postedCountdowns.json`(GitHub Contents API経由の書き込み) | X投稿の冪等性フラグ |
| `/api/cron/daily-digest` | Vercel Cron | `0 23 * * *` | 8:00 | 書き込みなし(`sendDigestEmail()`によるメール送信のみ) | |
| `daily-digest.yml` | GHA(無効) | schedule無し(`workflow_dispatch`のみ) | — | 書き込みなし(echoのみ) | メール送信はVercel Cronの`/api/cron/daily-digest`に移行済み。ワークフロー自体は無効化済みの記録として残置 |
| `warm-routes.yml` | GHA(`deployment_status`トリガー) | schedule無し | — | 書き込みなし(本番ルートへのGETのみ) | Production環境のdeployment_statusがsuccessの時のみ発火 |
| `actionlint.yml` | GHA(`pull_request`トリガー) | schedule無し | — | 書き込みなし(lintのみ) | `.github/workflows/**`変更時のみ |

**上表のJST列はcron式の宣言値(nominal)であり、実際の起動時刻ではない。** GitHub Actionsのscheduled実行はキュー混雑で遅延することがあり、`update-fighter-records.yml`の直近18回分の実行履歴(2026-07-07〜2026-07-24、`gh run list --workflow=update-fighter-records.yml`で取得)では、nominal cron時刻(UTC 17:30=JST 2:30)に対する実起動の遅延が**中央値3.27時間・最大5.25時間・最小2.68時間**だった(全18件が2〜5時間台の遅延で、定刻どおりに起動した回は無い)。**したがって「この時間帯を避ければ安全」という時刻ベースの回避は成立しない。** data/を書くジョブとの衝突回避は、時刻ではなく実行中run自体をGitHub Actions APIで照会する方式で行う(PR-G, #210)。
なお実行時間(起動後の所要時間)自体は安定しており、同じ18件の実測で中央値8.5分・最大13.8分・最小8.0分だった。

GitHub Actions・Vercel Cron以外の外部トリガー(webhook等)は無い。

**スケジュール未設定だが手動運用されているスクリプト(data/配下に書き込みうるもの)**:
- `scripts/update-mnews-rating.ts --mode=data-correction`: `data/rankings.json`, `data/rankings.prev.json`, `data/rankings/archive/*`, `data/rankings.legitimateBaseline.json`(条件付き)。同スクリプトはmode未指定(new-results)で上記2:30バッチからも呼ばれるため、手動実行のタイミング次第でバッチと競合しうる
- `scripts/update-rizin-records.ts`: `data/rizinRecords.json`。出力は2:30バッチ(`update-fighter-records.ts`・`update-mnews-rating.ts`)の入力として読まれるが、この生成スクリプト自体はどこからもスケジュール実行されていない
- `scripts/backfill-bout-weightclass.ts`: `data/fighterRecords.json`(2:30バッチと同じ出力ファイル)
- `scripts/strip-tracking-params-migration.ts`: `data/archive.json`(30分ごとバッチと同じ出力ファイル。ファイル名からは一回限りの移行用途と見られる)

**孤立・要注意**:
- `scripts/daily-digest.ts`: ファイル冒頭のコメントは「毎朝8:00(JST)にGitHub Actionsから実行」と記載しているが、実際にはどのworkflowからも呼ばれていない(2026-07-25、grep実測で確認)。実際のメール送信は上記`/api/cron/daily-digest`(`sendDigestEmail()`)が担っており、このスクリプトは別実装のまま孤立している。出力`data/tweet-digest.txt`はアプリ側のどこからも読まれていない

## 認証境界(重要)
- 認証必須は `/admin/*`(管理画面)と `/api/admin/*`(管理系API)のみ
- 一般ユーザーが使う機能は絶対に `/admin` 配下に置かない(middlewareに巻き込まれ一般ユーザーがログイン画面に飛ばされるデグレになる):
  - X投稿用カード作成ツール → `/tools/fighter-card`
  - OG/シェア画像生成API → `/api/og/*`
- `/api/revalidate-rankings`(2026-07-17追加): デプロイスクリプト等の自動実行から呼ぶ想定のため、意図的に`/api/admin/*`(Cookie/ブックマークURL認証)の外に置き、専用トークン`REVALIDATE_TOKEN`(`ADMIN_TOKEN`とは別、Vercel環境変数に別途設定)によるBearer認証のみで守っている。`/api/admin/*`側のmiddleware認証境界には一切手を加えていない(認証境界の変更は必ず事前確認を取ること)。

## クレデンシャル取り扱い(厳守)
- **既存の**トークン・APIキー・パスワード等クレデンシャルの生値は、目的を問わず読み出し・出力・ログ表示・エコーを一切禁止する(マスクした値の確認は可)
- 例外: **そのセッション内でClaude自身が新規生成したトークン**は、生値をstdout・ログ・報告文に一切表示しないという条件下でのみ取り扱ってよい(変数経由での置換・stdin渡し等)。既存クレデンシャルの読み出し禁止は変わらない
- 認証が必要な動作確認は「未認証時の挙動確認」までを自動で行い、認証後の画面確認は人間に依頼する。ただし人間が指定した使い捨ての非機密トークン(例: ローカル確認専用の固定文字列)を使う場合はこの限りではない
- クレデンシャルに触れる必要が生じた場合は、代替手段の有無にかかわらず作業を止めて確認を取る
- `.claude/settings.json` の `permissions.deny`(+ `sandbox.credentials.files`)で `.env` / `.env.local` へのRead・Bashアクセスを機械的にも遮断済み(2026-07-10)。運用ルールだけでなく設定でも二重に防止する

## コンテンツ取得・著作権ポリシー（必須遵守 / 例外なし）

### 取得ソースのルール
- **eFight（efight.jp）: 取得禁止。**
  規約で「蓄積」「スクレイピング（非正規手法での情報取得）」を明示的に禁止しており、かつ有料課金メディアのため。自動取得ジョブは稼働させない。既存の取り込み済みアイテムは表示側で除外する（取得ジョブ停止＋表示除外で対応）。
  DBからの物理削除はこのポリシーでは行わない（不可逆操作のため人間の明示判断で別途実施）。
- **ORICON（oricon.co.jp）: RIZIN関連のみ取得可。**
  取得元は格闘技タグ一覧（https://www.oricon.co.jp/news/tag/id/kakutougi/）。
  `RIZIN` / `ライジン` のキーワード一致でフィルタし、非RIZIN記事は除外する。
  選手名による追加フィルタは、手書きリストの保守コストを避けるため現時点では実装しない（保留）。
  将来必要になった場合は、FIGHTERS から org==RIZIN を動的に引く形で実装する（手書き選手名リストは作らない）。
  ORICONは規約上「リンクは自由・本文/画像の複製は禁止」→ 見出し＋元記事URLのみ格納。
- **MMAPLANET / ゴング格闘技 / その他商用メディア:**
  見出し＋元記事URLのみ。本文・画像は扱わない。

### 全ソース共通の取得ルール
- 保存してよいのは「見出しテキスト」＋「元記事URL」のみ。**本文・要約・画像は取得も保存もしない。**
- 見出しは元媒体のデッドコピー（丸写し）を避け、mnews側の言葉で書き直す。
- 表示は必ず元記事へのリンク送りにし、流入を元媒体へ返す（代替・競合しない）。
- 【根拠】ニュース見出し自体に著作権は認められにくいが、「鮮度の高い見出しを営利で反復継続してデッドコピーし、元媒体の業務と競合する形で配信する行為」は不法行為と判断された判例がある（読売オンライン見出し事件・知財高裁 平成17年10月6日）。書き直し＋リンク送りでこれを回避する。

### 選手画像ポリシー
- **選手写真の無断転載をしない。** 取得・保存・表示のいずれも不可。
  使用可なのは、主催公式プレスキット等で明示許諾がある画像、またはライセンス素材のみ。
- 選手・対戦カードのビジュアルは自作スタッツカード（画像非依存）で対応する。
  これは法的にクリーンかつ差別化要素になるため、方針として維持する。

## 大会予定（/events）登録ルール

大会予定に「入れる/入れない」は団体ごとに以下で判定する。配信ブランド（Lemino等の冠）ではなく**主催**で判定するのが原則。判断に迷ったらこのルールを正とし、大会ごとに再判断しない。

### 判定ルール（4団体）

- **RIZIN**：全興行を登録。ナンバーシリーズ、LANDMARK、TRIGGER 等すべて含む。
- **修斗**：サステイン主催のプロフェッショナル修斗公式戦（後楽園/ニューピア本興行＝Vol.ナンバー）＋ Lemino修斗のナンバー興行のみ。
  - 主催で判定する。地方主催（THE BLACK BELT JAPAN / Torao Nation State / SECOND OUT 等）は Lemino 冠が付いていても**除外**。
  - 例：`Lemino修斗～POUND OUT～`（THE BLACK BELT JAPAN 主催）は除外。
- **パンクラス**：PANCRASE ナンバー本興行のみ。地方・チャレンジ・アマ系は除外。
- **DEEP**：DEEP本体のナンバー興行（DEEP ○○ IMPACT 等）＋ DEEP TOKYO IMPACT シリーズ ＋ DEEP JEWELS。DEEP Fight Challenge・DEEP☆FUTURE・地方・アマ系は除外。

### 原則

- **主催ベース判定**：配信ブランドや大会名の冠ではなく、主催団体で本興行/地方興行を切り分ける。
- **網羅の対象は大会予定（1行）まで**。結果まとめの記事化は別基準（主要大会＋タイトルマッチ＋DB掲載選手が絡む試合）で選別し、全大会をフル記事化しない。
- データ捏造禁止（会場未確定等は空/未定で扱い、具体名を補完しない）。

## 大会結果（/results）掲載ルール

結果ページは**網羅**が正解。予定（/events）の選別ルールを結果に適用しない。予定は選別・結果は網羅、の非対称は意図的。

### 方針

- **実際に開催され結果データがある大会は、主催・規模を問わず掲載する**。地方主催（THE BLACK BELT JAPAN / Torao Nation State / SECOND OUT 等）・DEEP地方IMPACT・DEEP JEWELS 等も含め、結果は残す。
- 理由：
  1. 結果ページはインデックス済みの資産。削除は404/リダイレクトを生みSEO資産を毀損する。
  2. 地方カード×選手名の長尾検索が本サイトの差別化（モート）。地方結果を削るとこれを自ら失う。
  3. 結果は fighterRecords（選手戦績）に紐づく。地方大会の結果を削ると戦績に穴が空き、戦績の正確性が崩れる。

### 例外（削除してよいのは品質理由のみ）

- 「開催されたが結果が空／薄い」ページ（thin content）は削除または補完の対象。
- これは"主要大会以外を抜く"ではなく"中身が無いページを抜く"。判定基準が別であることに注意。

### 結果まとめ「記事」との区別

- 上記は結果データの**掲載可否**の話。フル recap 記事（editorial まとめ）を書くかどうかは別基準（主要大会＋タイトルマッチ＋DB掲載選手が絡む試合）で選別してよい。
- 構造化された結果リスト＝網羅、手書きまとめ記事＝選別。混同しない。

## 選手名フォントサイズの実装教訓(2026-07-22、PR #182→#184で同種のミスを2回反復)

- 要件は「全カード・全ページで選手名の大きさが同一」。**ページ内の一致とページ間の一致は二者択一ではなく、両方満たす必要がある**。どちらか一方だけを満たす実装(カード単体で決める/ページ単位で決める)は要件の取り違え。
- サイズが小さすぎる/大きすぎると感じた場合、**単一サイズの仕組み自体を壊すのではなく、まず計算式のバグを疑う**。実際の事故は `fighterNameSize()`(サイズ計算)が `renderWrappableName()`(実描画の折り返し挙動)とズレていたことが真因で、単一サイズ設計自体は正しかった。
- 全カード・全ページを1つの値(`GLOBAL_FIGHTER_NAME_SIZE`)で揃える設計は、**構造上「最も厳しい1件」に全体が引きずられる**(1名の表記・1件のバグでサイト全体が縮む)。この設計を採る場合、下限ガードと原因(選手名+大会slug)をビルド時に表示する仕組みは必須(`scripts/check-event-namesize-override.ts` の `GLOBAL_NAME_SIZE_FLOOR` 参照)。

## 選手追加PRのルール

- 選手追加PRには `data/fighterRecords.json` の該当エントリを同PRに含める(`fighters.ts`への追加だけでは戦績が「データなし」のままデプロイされるため)。

## 「数字で見る対戦カード」記事の公開手順

1. `/admin/drafts` タブ③で大会・対象試合を選び、「プレビュー」で内容を確認する。
2. 「コードをコピー」で出力された配列要素を `src/lib/originalArticles.ts` の `ORIGINAL_ARTICLES` に貼り付ける。
3. 通常のgitコミット(作業ブランチ推奨) → デプロイで公開する(この画面からの自動公開・自動git書き込みは行わない)。
4. デプロイ後 `/articles/[slug]` が正しく表示されることを確認する。
5. 「告知テキストをコピー」でX告知文をコピーし、記事URL有効化後に手動投稿する。

## docs/instructions/ の運用ルール

- 各指示書の冒頭に `status: active` / `status: superseded` / `status: done` のいずれかを明記する
- `superseded`(実装済みだが内容が古い) / `done`(役目を終えた) の指示書は**実装の根拠として参照しない**。現行実装の正はコードと本CLAUDE.mdであり、指示書はあくまで作成時点の設計意図の記録
- 実装が指示書の内容から変わった場合、指示書を都度全面書き換えする必要はない。冒頭にstatusと簡単な補足(何が現行と異なるか)を付ければ足りる
