# メール送信はVercel Cronに移行済み（/api/cron/daily-digest）
# このワークフローは無効化されています
name: Daily digest email (disabled)

on:
  workflow_dispatch: {}

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel Cronに移行済みのため、このワークフローは実行されません。"
